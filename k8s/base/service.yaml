apiVersion: v1
kind: Service
metadata:
  name: sovereign-comm-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: sovereign-comm
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 80
      targetPort: http
